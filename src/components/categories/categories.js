import React, { Component } from 'react';
import { connect } from 'react-redux';

import './categories.css'

export default class Categories extends Component {

    render() {
        return(
            <div className = "categories-list">
                <p>listi yfir categories</p>
            </div>
        )
    }
}